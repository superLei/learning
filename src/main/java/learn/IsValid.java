package learn;

public class IsValid {
    public boolean isValid(String s) {
        if(s == null || s.length() < 1) {
            return true;
        }
        int len = s.length();
        if(len %2 != 0){
            return false;
        }else {
            for (int i = 0; i < len; i++) {
                
            }
        }

    }
}
